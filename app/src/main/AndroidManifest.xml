<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--相机权限-->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!--外部存储权限-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <!--版本10+需要-->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!--设备特性：需要相机-->
    <uses-permission android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_sj"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31"
        >

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PublicAlbumActivity" android:exported="false"/>
        <activity android:name=".PrivateAlbumActivity" android:exported="false"/>
        <activity android:name=".GridSpacingItemDecoration" android:exported="false"/>
        <activity android:name=".Photo" android:exported="false"/>
        <activity android:name=".ImageDetailActivity" android:exported="false"/>
        <activity android:name=".CameraTestActivity" android:exported="false"/>
        <activity android:name=".ImageCropActivity" android:exported="false"/>
        <activity android:name=".NewCropActivity" android:exported="false"/>

    </application>

</manifest>